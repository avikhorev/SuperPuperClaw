services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
      - claude_auth:/home/botuser/.claude
      - ./youtube_cookies.txt:/app/youtube_cookies.txt:ro
    environment:
      - DATA_DIR=/data

volumes:
  claude_auth:
