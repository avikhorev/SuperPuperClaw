services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
      - ${HOME}/.claude:/home/botuser/.claude:ro
    environment:
      - DATA_DIR=/data
